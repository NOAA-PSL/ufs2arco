name: ufs2arco
channels:
    - conda-forge
dependencies:
    - python=3.11
# Basics
    - numpy
    - matplotlib
# xarray et al
    - xarray
    - netCDF4
    - h5netcdf
    - bottleneck
    - dask[complete]
    - zarr
    - cftime
# jupyter etc
    - jupyter
    - ipython
    - ipykernel
# Testing
    - pytest
    - coverage
# filesystem
    - fsspec
    - s3fs
    - gcsfs
# docs
    - nbsphinx
    - sphinx-book-theme
